apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: propup-cert
spec:
  dnsNames:
    - staging.openmanage.ai
  secretName: staging-openmanage-tls
  issuerRef:
    name: cert-issuer
    kind: ClusterIssuer
